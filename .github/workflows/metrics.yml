name: Claude Code Metrics

on:
  schedule:
    - cron: '0 14 * * 5'
  workflow_dispatch:

jobs:
  metrics:
    uses: KapivaTech/actions-workflows/.github/workflows/metrics-shared.yml@main
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
